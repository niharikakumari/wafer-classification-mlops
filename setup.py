from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Predicting if the wafer is working (a component of semiconductors) or not, building everything utilising the principles of MLOps',
    author='niharika_kumari',
    license='MIT',
)
